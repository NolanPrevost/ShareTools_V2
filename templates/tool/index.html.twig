{% extends 'base.html.twig' %} {% block title %}Tool index{% endblock %} {%
block body %}
<h1>Tool index</h1>
<a href="{{ path('app_tool_new') }}">Create new</a>
<div class="row row-cols-1 row-cols-md-3 g-4">
  {% for tool in tools %}
  <div class="col">
    <div class="card">
      <img src="{{ tool.image }}" class="card-img-top" alt="..." />
      <div class="card-body">
        <h5 class="card-title">{{ tool.toolName }} - {{ tool.id }}</h5>
        <p class="card-text">
          This is a longer card with supporting text below as a natural lead-in
          to additional content. This content is a little bit longer.
        </p>
        <a href="{{ path('app_tool_show', { id: tool.id }) }}">show</a>
        <a href="{{ path('app_tool_edit', { id: tool.id }) }}">edit</a>
      </div>
    </div>
  </div>
  {% endfor %}
</div>
{% endblock %}
